let topSongs = ['First Time', 'Hello', 'No Love', 'GoodBye'];

// push - add to end
topSongs.push('Balance');
console.log(topSongs);

// pop - remove from end
const nextSong = topSongs.pop();
console.log(nextSong);
