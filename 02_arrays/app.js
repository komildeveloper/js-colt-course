// Array.sort()
let people = ['Mrs. Robinson', 'Angie', 'Jolene', 'Maggie May', 'Roxanne']

console.log(people); // unsorted
console.log(people.sort()); // sorted

let nums = [34, 10, 100000, 67, 99];
console.log(nums.sort());
